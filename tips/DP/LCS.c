#include <stdio.h>
#include <string.h>
#define N 20

int tmp[N][N]={0};

int LCS(char *ch1,char *ch2)
{
	int lenth1,lenth2;
	int i,j;
	lenth1=strlen(ch1);
	lenth2=strlen(ch2);
	for(i=0;i<=lenth1;i++)
		
}
